<div class="comment-wrapper">
  <h1>コメント一覧</h1>
  <% @comments.each do |c| %>
    <div>
      <br>
      <%= c.content %>
    </div>
      <br>
  <% end %>

    <%= form_with(model: [@dogs, @comments], local: true) do |f| %>
      <%= f.text_area :content %>
      <%= button_tag type: "submit" do %>
        <i class="far fa-comments"></i> コメントする
      <% end %>
    <% end %>
 
</div>


<%= link_to "一覧に戻る", dogs_bird_path %>